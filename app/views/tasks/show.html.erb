<div class='container'>
    <br/>
    <h1><%= @task.title %></h1>
    <p><%= @task.body %></p>
    <p><%= @task.deadline %></p>

    <%= link_to 'Home', '/', class: 'btn btn-primary'%>
    <%= link_to 'Edit', edit_task_path(@task), class: 'btn btn-primary'%>
    <%= link_to 'Delete', @task, method: :delete, data: { confirm: "Are you sure?" }, class: 'btn btn-danger' %>
</div>